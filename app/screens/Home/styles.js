import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerCartBtn: {
    padding: 16,
  },
  header: {
    flex: 1,
    paddingLeft: 8,
  },
});
export default styles;
