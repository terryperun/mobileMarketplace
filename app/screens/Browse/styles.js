import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  header: {
    flex: 1,
    paddingLeft: 16,
  },

  rightIcon: {
    paddingRight: 16,
  },
});
export default styles;
