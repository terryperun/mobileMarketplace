import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  header: {
    flex: 1,
    paddingLeft: 16,
    paddingRight: 16,
    // position: 'absolute',
    // zIndex: 1,
    // top: 60,
    // left: 0,
    // right: 0,
  },
});
export default styles;
